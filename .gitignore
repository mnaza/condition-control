# PlatformIO
.pio/
.vscode/

# Rust (firmware-stick-rs)
target/
.embuild/

# Wi-Fi / MQTT credentials (copy secrets.example.h -> secrets.h)
secrets.h

# Internal planning/design notes — kept local, not published
docs/superpowers/
